target/
*.spkg

# Regenerated by `substreams build` on every run. The crate compiles protobuf through build.rs
# into OUT_DIR instead, so this tree is never referenced by lib.rs.
buf.gen.yaml
src/pb/
